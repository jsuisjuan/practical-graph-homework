#include <iostream>
#define MAX 10

const char* teste = "juan";

/* void criar_grafo_matriz_adjacente(int grafo[][MAX], int quantidade_vertice){
    for (int i = 0; i < quantidade_vertice; i++)
        for (int j = 0; j < quantidade_vertice; j++)
            std::cin>>grafo[i][j];
} */

int main(int argc, char *argv[]){
    int grafo[MAX][MAX], quantidade_vertice;
    std::cout<<"quantidade do vertice: "<<teste;
    /* std::cin>>quantidade_vertice;
    std::cout<<"escreva o grafo:"<<std::endl;
    criar_grafo_matriz_adjacente(grafo,quantidade_vertice); */
    
    return 0;
}
